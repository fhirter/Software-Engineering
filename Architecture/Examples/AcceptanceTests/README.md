# README

1. Gauge installieren. Siehe https://docs.gauge.org/getting_started/installing-gauge
2. Gauge initialisieren: `gauge init python`
3. Playwright installieren: `pip install pytest-playwright`
4. Playwright browser installieren: `playwright install`
5. Tests mit `gauge run specs` ausführen